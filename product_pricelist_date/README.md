Pricelist
=========

This module allows get the rule that has the most current date.
Also contains a patch that modifies the query of the item,
return to the rule depending on _order defined in the class.