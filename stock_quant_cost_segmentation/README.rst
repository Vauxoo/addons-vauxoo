Stock Quant Cost Segmentation
=============================

This module allows to make segmentation of the cost on each quant have four new
fields `material_cost`, `production_cost`, `subcontracting_cost` and
`landed_cost`.
