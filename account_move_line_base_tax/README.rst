Amount Base Account Move Line
=============================

This module adds the fields in account_move_line model:

- `amount_base`
- `tax_id_secondary`

These fields are fill when you validate the invoice.
